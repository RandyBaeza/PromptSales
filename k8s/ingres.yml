apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: campaigns-ingress
  annotations:
    kubernetes.io/ingress.class: alb
spec:
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: campaigns-api-service
                port:
                  number: 3000